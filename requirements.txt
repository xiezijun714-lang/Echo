# Core Python runtime dependencies, pinned to the versions used for the paper.
# NOTE: This file does NOT cover the CUDA-compiled stack (torch / sglang /
# megatron-core / transformer_engine / flash_attn / flashinfer). Those must be
# installed in a specific order against your CUDA toolkit — see README.md and
# requirements-cuda.txt. Install Echo itself with `pip install -e .`.
accelerate==1.13.0
codetiming==1.4.0
datasets==4.8.4
dill==0.4.1
einops==0.8.2
fastapi==0.135.2
hydra-core==1.3.2
latex2sympy2_extended
math_verify
ninja==1.13.0
numpy==1.26.4
packaging==25.0
pandas==2.3.3
peft==0.18.1
pillow==12.1.1
pyarrow==23.0.1
pybind11==3.0.3
pylatexenc==2.10
ray[default]==2.54.1
tensorboard==2.20.0
tensordict==0.10.0
torchdata==0.11.0
transformers==4.57.0
uvicorn==0.42.0
wandb==0.25.1
